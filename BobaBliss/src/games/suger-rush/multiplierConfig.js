/*
 * games/suger-rush/multiplierConfig.js — DEAD-CODE COMMENTED OUT 2026-06-22.
 *
 * Empty placeholder for a per-game multiplier config that was
 * never authored. The shipped game's multiplier ladder lives
 * under `gameConfig.multipliers.levels` in `config.js`; the
 * rendering is computed inline by `pixi/PixiOverlay.jsx`.
 *
 * Original (empty) file:
 */

// (intentionally left blank — see header above)