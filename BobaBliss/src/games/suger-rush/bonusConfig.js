/*
 * games/suger-rush/bonusConfig.js — DEAD-CODE COMMENTED OUT 2026-06-22.
 *
 * Empty placeholder for a per-game bonus config that was never
 * authored. The shipped game reads its scatter / free-spin config
 * from `gameConfig.bonus.scatterTrigger` and
 * `gameConfig.freeSpins.awards` in this same folder's `config.js`.
 *
 * Original (empty) file:
 */

// (intentionally left blank — see header above)