/*
 * games/suger-rush/symbols.js — DEAD-CODE COMMENTED OUT 2026-06-22.
 *
 * Empty placeholder for a per-game symbol table that was never
 * authored. The shipped game reads its symbol set from
 * `gameConfig.symbols` and the per-symbol asset URLs from
 * `gameConfig.assets.symbols` in `config.js`. The
 * `pixi/symbolRegistry.js` module re-exports both.
 *
 * Original (empty) file:
 */

// (intentionally left blank — see header above)