/*
 * GameManager.js — DEAD-CODE COMMENTED OUT 2026-06-22.
 *
 * Was a planned orchestrator for multiple game instances that was
 * never built. The single-game path goes through `SlotEngine` (see
 * `engine/SlotEngine.js`) and `AssetRegistry` (see
 * `loaders/AssetRegistry.js`); there is no second consumer of a
 * "GameManager" anywhere in `src/`.
 *
 * Original (empty) file:
 */

// (intentionally left blank — see header above)