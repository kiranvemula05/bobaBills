/*
 * components/config/paytable.js — DEAD-CODE COMMENTED OUT 2026-06-22.
 *
 * Stub `A / K / Q` paytable from an early prototype. The active
 * game's paytable lives in `games/suger-rush/paytable.js` and is
 * imported directly by `WinCalculator` and `PaytablePopup`. Nothing
 * in `src/` imports `PAYTABLE` from this file.
 *
 * Original contents preserved below for future restoration:
 */

/*
export const PAYTABLE = {
    A: {
        8: 5,
        10: 10,
        12: 20,
        15: 50
    },

    K: {
        8: 4,
        10: 8,
        12: 15,
        15: 40
    },

    Q: {
        8: 3,
        10: 6,
        12: 12,
        15: 30
    }
};
*/